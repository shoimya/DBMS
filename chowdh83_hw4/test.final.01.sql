1: CREATE TABLE names (name TEXT, id INTEGER);
2: CREATE TABLE IF NOT EXISTS names (name TEXT, id INTEGER);
1: INSERT INTO names VALUES ('James', 1);
2: INSERT INTO names VALUES ('SHOM', 2);
1: INSERT INTO names VALUES ('NOWREEN', 1);
2: INSERT INTO names VALUES ('RDMN', 2);
1: SELECT * FROM names ORDER BY id;
1: BEGIN TRANSACTION;
2: BEGIN TRANSACTION;
2: INSERT INTO names VALUES ('SHOM FROM TRAN 2', 2);
1: INSERT INTO names VALUES ('SHOM FROM TRAN 1', 1);
2: SELECT * FROM names ORDER BY id;
2: ROLLBACK TRANSACTION;
1: SELECT * FROM names ORDER BY id;